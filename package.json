{
  "name": "pep",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss -i ./input.css -o ./src/styles.css --watch",
    "prod:css": "npx tailwindcss -i ./input.css -o ./src/styles.css --minify",
    "deploy": "npm run prod:css && firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
